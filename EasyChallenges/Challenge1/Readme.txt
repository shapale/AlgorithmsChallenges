Challenge #1

Write the function which takes in a non-empty arra of integers and an integer representing a target sum.
If any two numbers of the array are sum up to the target sum then the function should return the array including these two numbers. 
Otherwise return the empty array.  

Input:

array = [3, 5, -4, 8, 11, 1, -1, 6]
targetSum = 10